import React from 'react';
import { StyleSheet, View } from 'react-native';

const ListSeparator = () => <View style={styles.wrapper} />;

const styles = StyleSheet.create({
  wrapper: {
    height: 15,
  },
});

export { ListSeparator };
